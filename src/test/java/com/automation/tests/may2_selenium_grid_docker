AWS provides different cloud solutions, such as solutions to compute something, provide storage, databases.
It's a cloud system, where companies rent servers and can run web applications,databases.. Web applications
are hosted in a servers. You can rent a server based on your needs.

EC2 instance is a virtual server.

SSH- secure shell connection